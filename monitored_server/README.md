this image use basic prometheus configuration .
if you want to customize prometheus configuration , you can override the file prometheus.yml in located in /Prometheus/prometheus.yml on your container. 
